/*--------Navigate to SignUp Page--------------*/
function navigateTo(page) {
    if (page==='login'){
        window.location.href="LogIn.html"
    }
}

/*--------------Sign Up----------------------------*/

function SignUp() {
    const username =$("#")
}